Contains a simple script used to rename all selected files within a folder. To use, place script within the folder of interest that contains all the files you want to rename. Run the script and select all the files that you need to rename with the appropriate items you want to append and follow the remaining instructions. By default, the script appends the new name at that start of all filenames. 
